2026-03-05 Issue 1 COMPLETE: Created pspsps Xcode project. macOS 14.0, Swift 6.0, LSUIElement=true, sandbox disabled, audio input entitlement. WhisperKit resolved at 0.16.0. FluidAudio AVAILABLE — resolved from branch main (3e1ee8e), product FluidAudio added to main target. Both build and build-for-testing pass.
2026-03-05 Issue 2 COMPLETE: Created all protocol definitions and data models. Core/Protocols: ASREngine, PostProcessor, AudioInputSource. Core/Models: TranscriptionResult, PostProcessContext, ASRError, PostProcessError. PostProcessors/PassthroughPostProcessor. pspspsTests/Mocks/MockASREngine. All registered in project.pbxproj. xcodebuild build and build-for-testing pass. Issues 3, 4, 8 unblocked (READY).
2026-03-05 Issue 3 COMPLETE: Implemented AppConfig struct (pspsps/AppConfig.swift) with all PRD Section 9.1 fields and nested enums (ASREngineOption, PostProcessorOption, HotkeyMode — all Codable and CaseIterable). UserDefaults persistence via JSON encoding under single key "AppConfig". Static current accessor and load(from:)/save(to:) methods for testability. AppConfigTests.swift with 4 tests (encode/decode roundtrip, default values, empty-defaults fallback, CaseIterable). All 5 tests pass. Issues 6, 9, 18 unblocked (READY).
2026-03-05 Issue 4 COMPLETE: Implemented Audio/AudioProcessor.swift with normalizeRMS(_:targetDBFS:) per PRD Section 7.2 — computes RMS, applies gain clamped at +26 dB (20.0 linear), writes to new AVAudioPCMBuffer allocation. Created pspspsTests/Helpers/SyntheticAudio.swift with makeSineBuffer and makeSilenceBuffer. AudioProcessorTests.swift with 5 tests (sine -40 dBFS → -20 dBFS, silence no-op, gain clamp, format/frame-count match, new allocation). All 10 tests pass. Issue 5 unblocked (READY).
2026-03-05 Issue 5 COMPLETE: Implemented Audio/AudioCaptureManager.swift (AVAudioEngine wrap, AudioInputSource conformance, 16kHz mono Float32 output, NSLock-protected buffer accumulation, max-duration Timer on RunLoop.main, onAutoStop callback) and Audio/AudioDeviceManager.swift (CoreAudio device enumeration via kAudioHardwarePropertyDevices, input-channel filtering, UID lookup, system-default fallback). Both registered in project.pbxproj. xcodebuild build passes.
2026-03-05 Issue 6 COMPLETE: Implemented ASR/WhisperKitEngine.swift (ASREngine conformance, loadModel via WhisperKit init with custom downloadBase in ~/Library/Application Support/pspsps/models/whisperkit/, unloadModel nils reference, transcribe uses wk.transcribe(audioArray:) with manual float array conversion since asFloatArray() is internal). Implemented ASR/ModelDownloadManager.swift (@MainActor ObservableObject, ModelState enum with notDownloaded/downloading(progress:)/downloaded/failed, downloadWhisperKit via WhisperKit.download, isDownloaded checks disk, parakeetState stub). Both registered in project.pbxproj. xcodebuild build passes. Issues 7 and 16 unblocked (READY).
2026-03-05 Issue 7 COMPLETE: Generated pspspsTests/TestFixtures/test_speech.wav (16kHz mono WAV, macOS TTS "The quick brown fox jumps over the lazy dog"). Created pspspsTests/WhisperKitIntegrationTests.swift with two tests: model loads without error, transcription returns ≥3 recognizable words from phrase. Added TestFixtures group + Resources build phase to test target in project.pbxproj. Fixed WhisperKitEngine model name (was hardcoded "openai_whisper-large-v3-turbo" which doesn't exist in the repo; now uses nil → device-recommended model). Both integration tests pass (316s total, model downloaded ~600MB). Issue 10 unblocked (READY).
2026-03-05 Issue 8 COMPLETE: Implemented Input/HotkeyManager.swift — CGEvent tap registered on cgSessionEventTap with headInsertEventTap placement. Matches keyCode + modifier flags (masked to shift/control/option/command). Returns nil to consume matched events. Push-to-talk mode: keyDown→.started, keyUp→.stopped (autorepeat ignored). Toggle mode: keyDown toggles active state, fires .started/.stopped. AXIsProcessTrusted() checked in startListening() before tap creation, exposed via isPermissionGranted(). HotkeyEvent enum has .started and .stopped. Tap auto-re-enabled on tapDisabledByTimeout/tapDisabledByUserInput. xcodebuild build succeeds.
2026-03-05 Issue 9 COMPLETE: Implemented Input/TextPaster.swift — saves current pasteboard string, writes new text, synthesizes Cmd+V via CGEvent (virtual key 0x09 with .maskCommand), restores previous pasteboard after AppConfig.pasteboardRestoreDelaySeconds (default 0.3s) via DispatchQueue.main.asyncAfter. TextPasterTests.swift (@MainActor, 3 async tests): pasteboard write check, restore-after-delay, and default AppConfig delay. All tests pass. No issues were directly blocked by Issue 9.
2026-03-05 Issue 11 COMPLETE: Implemented AppCoordinator.swift (@MainActor ObservableObject, AppState enum: idle/recording/transcribing, holds HotkeyManager+AudioCaptureManager+TranscriptionPipeline+TextPaster). State transitions: idle→recording on hotkey .started, recording→transcribing on hotkey .stopped/auto-stop, transcribing→idle on paste completion or error. All errors logged via OSLog, not thrown. Model loaded async at startup. Updated pspspsApp.swift to use @NSApplicationDelegateAdaptor(AppDelegate) which creates coordinator on applicationDidFinishLaunching. Added Sendable to ASREngine protocol; marked WhisperKitEngine+MockASREngine as @unchecked Sendable for Swift 6 conformance. All 19 unit tests pass. Issue 12 unblocked (READY).
2026-03-05 Issue 10 COMPLETE: Implemented Core/Pipeline/TranscriptionPipeline.swift (@MainActor final class — holds currentTask: Task<String, Error>?, cancels it on each run() call, normalizes audio via AudioProcessor.normalizeRMS before transcription, post-processes result) and Core/Pipeline/PipelineFactory.swift (@MainActor struct — builds WhisperKitEngine+PassthroughPostProcessor from AppConfig; Ollama/Parakeet stubs pending Issues 15/16). Added transcribeDelay to MockASREngine for cancellation testing. TranscriptionPipelineTests.swift with 5 tests: mock engine returns expected text, cancellation of in-flight task, PipelineFactory builds correct engine and post-processor, normalization path. All 5 tests pass. Issues 11 and 15 unblocked (READY).
2026-03-05 Issue 13 COMPLETE: Implemented UI/RecordingOverlay.swift — NSPanel with .nonactivatingPanel styleMask, .floating level, [.canJoinAllSpaces, .fullScreenAuxiliary] collectionBehavior. Subscribes to AppCoordinator.$state via Combine; shows "Recording..." / "Transcribing..." / truncated result (40 chars). Auto-dismisses via Task.sleep after AppConfig.overlayDurationSeconds. Positioned at bottom-center of active screen. Added @Published lastTranscript to AppCoordinator, set before state→idle. Updated MenuBarController popover (MenuBarPopoverView) to observe coordinator and show lastTranscript + "Copy & Paste" button (calls TextPaster.paste). Wired RecordingOverlay in AppDelegate. Registered in project.pbxproj (UUIDs EC/ED). xcodebuild build succeeds.
2026-03-05 Issue 12 COMPLETE: Implemented UI/MenuBarController.swift — NSStatusItem with mic SF Symbol icon, icon state machine (idle: mic template, recording: mic.fill red tint, transcribing: waveform template), left-click placeholder popover (MenuBarPopoverView SwiftUI), right-click context menu (Settings/openSettingsWindow, History placeholder, Quit/terminate). Updated AppDelegate to create and hold MenuBarController alongside AppCoordinator. Registered UI group + MenuBarController.swift in project.pbxproj. xcodebuild build succeeds. Issues 13 and 14 unblocked (READY).
2026-03-05 Issue 14 COMPLETE: Implemented UI/Settings/SettingsView.swift with all 5 tabs (General, ASR Engine, Post-Processing, Audio, History). General: hotkey recorder (HotkeyEventMonitor with @unchecked Sendable), mode picker, SMAppService launch-at-login, sound feedback toggle. ASR Engine: engine rows with download state from ModelDownloadManager, download/delete/switch buttons, "Recommended" badge for WhisperKit, "Coming soon" for Parakeet. Post-Processing: Ollama/None picker, model+host fields, test connection button (URLSession GET /api/tags). Audio: device picker from AudioDeviceManager, gain toggle+slider, max duration stepper, live MicLevelMonitor (AVAudioEngine tap). History: toggle, max items stepper, clear button placeholder. Added @Published var config to AppCoordinator with didSet save(), ModelDownloadManager as let property, Combine observation to rebuild pipeline on ASR engine/postprocessor change and restart hotkey listener on hotkey changes. Updated pspspsApp to use SettingsView with environmentObject injections; coordinator is now a let (non-optional) in AppDelegate. Registered Settings group + SettingsView.swift in project.pbxproj. xcodebuild build succeeds, all 19 unit tests pass.
2026-03-06 Issue 15 COMPLETE: Implemented PostProcessors/OllamaPostProcessor.swift — HTTP POST to {host}/api/chat with stream:false, PRD Section 6.1 system prompt (6 cleanup rules + 4 Do NOT rules), user message "Active app: {app}\nTranscript: {text}", parses response.message.content. checkAvailability() does GET /api/tags and verifies model name is in list. On any failure, logs and returns raw transcript (PassthroughPostProcessor behavior). URLSession is injectable for testing. OllamaPostProcessorTests.swift uses MockURLProtocol with nonisolated(unsafe) static handler and httpBodyStream reconstruction. PipelineFactory updated to use OllamaPostProcessor for .ollama case. All 25 unit tests pass (7 new Ollama tests).
2026-03-06 Issue 20 COMPLETE: Implemented all remaining error handling. Intel arch check (#if !arch(arm64)) in applicationDidFinishLaunching shows alert and terminates on non-ARM Macs. AppCoordinator: added showToast PassthroughSubject (sync delivery) for overlay messages; empty transcription shows "Nothing detected" toast (no paste); paste failure (AXIsProcessTrusted() returns false) writes to clipboard and shows "Copied to clipboard" toast; max duration auto-stop shows "Max duration reached" toast; audio device disconnect (AVAudioEngineConfigurationChange) shows "Audio device disconnected" and goes idle; mic denied shows toast on startup; model load failure sets modelNotLoaded=true; accessibility not granted sets accessibilityNotGranted=true. TextPaster.paste() returns Bool and checks AXIsProcessTrusted() before synthesizing Cmd+V. RecordingOverlay now subscribes to coordinator.showToast (sync) for result display, state observer only dismisses on non-transcribing idle transitions. MenuBarController shows lock icon for accessibilityNotGranted, warning triangle for modelNotLoaded, normal state icons otherwise. AudioCaptureManager observes AVAudioEngineConfigurationChange notification and fires onDeviceDisconnected callback. Sound feedback: NSSound Tink on start, Pop on stop/auto-stop, respects soundFeedbackEnabled. All 31 unit tests pass, build succeeds.
2026-03-06 Issue 19 COMPLETE: Implemented full onboarding flow. OnboardingView.swift orchestrates 3 steps: (1) ASRPickerView with onContinue callback, (2) AccessibilityPermissionView (polls AXIsProcessTrusted() every 500ms via Timer.publish, button opens System Settings Accessibility pane, auto-advances when granted), (3) MicrophonePermissionView (AVCaptureDevice.requestAccess, advances immediately if already authorized). AppDelegate.showOnboardingWindow() creates NSWindow with NSHostingView wrapping OnboardingView; shown only when hasCompletedOnboarding is false in UserDefaults; sets key and closes window on completion. Issue 20 unblocked (READY). xcodebuild build succeeds, all 31 unit tests pass.
2026-03-06 Issue 18 COMPLETE: Implemented History/TranscriptEntry.swift (Codable struct: id, text, timestamp, sourceApp, sourceAppBundleID) and History/TranscriptHistory.swift (@MainActor ObservableObject, persists JSON to ~/Library/Application Support/pspsps/history.json, add/delete/search/clear with maxHistoryItems trim, injectable fileURL for tests). Implemented UI/HistoryPopoverView.swift (SwiftUI list with search bar, timestamp+preview rows, tap-to-repaste, swipe-to-delete). Updated MenuBarController to add historyPopover (NSPopover) wired to "History" context menu item via @objc showHistory(). Updated AppCoordinator to capture frontmost app at recording start, add TranscriptHistory instance, record entries after successful transcription (respects keepTranscriptHistory config). Registered History group + HistoryPopoverView + TranscriptHistoryTests in project.pbxproj (UUIDs FA–102). All 31 unit tests pass. Issues 19 (READY) was already READY.
2026-03-06 Issue 17 COMPLETE: Implemented UI/Onboarding/ASRPickerView.swift — two engine cards (WhisperKit pre-selected with "Recommended" badge, Parakeet enabled since FluidAudio resolved). parakeetEngineAvailable=true; if false, Parakeet card shows "Coming soon" and is disabled. Download & Continue button triggers ModelDownloadManager, progress bar replaces button while downloading, Continue button appears after download. Engine switch via coordinator.config.asrEngine change triggers rebuildPipeline() which calls unloadModel()/loadModel(). Updated SettingsView ASR Engine tab: removed Parakeet "Coming soon" label and disabled-download state (now that Issue 16 made Parakeet real). Registered Onboarding group + ASRPickerView.swift in project.pbxproj (UUIDs F7/F8/F9). Build succeeds, all 25 unit tests pass. Issue 19 unblocked (READY).
2026-03-06 Issue 16 COMPLETE: Implemented ASR/ParakeetEngine.swift using FluidAudio AsrManager/AsrModels API (FluidAudio was available). loadModel() calls AsrModels.loadFromCache() then AsrManager.initialize(models:), unloadModel() calls cleanup() and nils manager, transcribe() calls asrManager.transcribe(_:source:.microphone) returning language "en". Updated ModelDownloadManager.downloadParakeet() to call AsrModels.download(to:), isDownloaded(.parakeet) to use AsrModels.modelsExist, deleteModel(.parakeet) to remove model directory. Updated PipelineFactory to use ParakeetEngine() for .parakeet case. Registered in project.pbxproj (UUIDs F5/F6). xcodebuild build succeeds, all 25 unit tests pass. Issue 17 unblocked (READY).
